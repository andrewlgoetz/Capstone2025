# Copy this file to .env and fill in your local values
# Never commit .env, only .env.example

# Local PostgreSQL connection string (Copy this)
DATABASE_URL=postgresql+psycopg2://myuser:mypassword@localhost:5432/inventory_db

# Secret key for signing JWT tokens (can be any random string for local dev)
# Generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=lZCAGgBohwND3Lm3korKlEfPXaDUGLlpeKC7L673Ezw
